argo-cd:
  redis:
    serviceAccount:
        create: true
        name: redis

  controller:
    args:
      repoServerTimeoutSeconds: "180"

  server:
    ingress:
      hosts:
        - "rsp2.cis.gov.pl"


  openshift:
     enabled: true
